import React from 'react';
import {ScrollView, View} from 'react-native';
import StyledText from '../../commons/StyledText';
import theme from '../../commons/theme';

const Home = () => {
  return (
    <ScrollView>
      <StyledText>sdlfksdjflkj</StyledText>
    </ScrollView>
  );
};

export default Home;
